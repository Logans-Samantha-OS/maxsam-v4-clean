import LeadBankDashboard from '@/components/lead-bank/LeadBankDashboard';

export const metadata = {
  title: 'Lead Bank | MaxSam V4',
  description: 'Park leads that are not ready for outreach'
};

export default function LeadBankPage() {
  return <LeadBankDashboard />;
}
