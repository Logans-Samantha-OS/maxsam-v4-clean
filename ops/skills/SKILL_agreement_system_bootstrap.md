# SKILL_agreement_system_bootstrap

Bootstrap agreement primitives before full provider integration.

## Deliverables
- `agreements` table with lifecycle statuses
- `/api/send-agreement` stub endpoint
- task rows for follow-up workflow handoff

## Constraints
- No paid-provider dependency required
- Must remain compatible with existing agreement packet flows
